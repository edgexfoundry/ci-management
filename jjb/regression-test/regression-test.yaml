---

- project:
    name: regression-test
    project-name: regression-test
    project: regression-test
    mvn-settings: regression-test-settings
    stream:
      - 'master':
          branch: 'master'
    jobs:
      - '{project-name}-verify-pipeline':
          status-context: '{project-name}-verify-pipeline'
      - '{project-name}-merge-pipeline'
