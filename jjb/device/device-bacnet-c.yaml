---
- project:
    name: device-bacnet-c
    project-name: device-bacnet-c
    project: device-bacnet-c
    mvn-settings: device-bacnet-c-settings
    stream:
      - 'master':
          branch: master

    jobs:
      - '{project-name}-verify-pipeline':
          status-context: '{project-name}-verify-pipeline'
      - '{project-name}-merge-pipeline'
      - '{project-name}-pipeline-webhooks'